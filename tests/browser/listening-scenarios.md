# Listening normal-route preview

Start tests/support/listening-preview.ts with free PORT/WEB_PORT, then Vite with MUSICLATTE_PREVIEW_API_TARGET set to the matching loopback API origin. Authentication inputs remain in auth-harness, not evidence.

The preview serves an original two-second PCM tone and matching duration through the real API. Login → Music → Play random songs → Recent listening yields one qualified event. Play the same song again and verify one unique history card with only its latest time badge while Frequently played retains the authoritative count2. PREVIEW_CONTROL=seed adds54 synthetic events with repeated and unique songs, one missing lookup, half older than7 days, and50+4 raw-event pagination. The history and top projections hide the missing item, keep the newest visible card/order, and merge older duplicates after Load more.

Modes: missing makes current song lookup return not-found; error returns503; loading stalls lookup; record-error injects a synthetic event-insert failure; uncertain drops a submitted upstream connection; unauthorized fails scrobble authentication. normal restores ordinary responses. Only disposable test storage is affected. Do not copy authentication query strings or real music into evidence.

Verify the common Songs heading/toggle, compact one-row period/select/refresh controls, list and transparent tile frames, latest time badge, and each visible row's Play/Metadata/Favorite action columns. Play now, Add to queue, and row activation must all use the visible unique order and position before and after pagination. A raw page containing only missing songs shows one page-level unavailable status while retaining Load more; no per-event missing row is rendered. List errors retain the player, recording errors coexist with ongoing playback, repeat-one can exercise short audio continuously, and401 returns to login and removes the player. Another synthetic account has empty private history. KO/EN controls and dates retain card identities. Check desktop,390px,320px, and200% zoom without horizontal overflow or player overlap. Final visual/VoiceOver/Safari acceptance remains deferred.
